/**
 * @swagger
 * components:
 *   schemas:
 *     IDBody:
 *       required:
 *         - id
 *       properties:
 *         id:
 *           type: string
 */
export interface IDBody {
    id: string;
}